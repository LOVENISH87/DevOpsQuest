version: '3.8'
services:
  frontend:
    image: nginx:alpine
    ports:
      - "80:80"
  backend:
    image: node:18-alpine
    command: tail -f /dev/null
  database:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root