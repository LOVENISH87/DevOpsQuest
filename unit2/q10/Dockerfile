# Simulating a private registry distribution target
FROM alpine:latest

# Instructor commands for secure distribution:
# 1. docker login registry.example.com (uses auth token)
# 2. docker tag privateimage:latest registry.example.com/team/privateimage:latest
# 3. docker push registry.example.com/team/privateimage:latest

CMD ["echo", "Ready for Private distribution."]
