version: '3.8'

# Persistent volume allocation
services:
  data_processor:
    image: python:3.9-slim
    command: tail -f /dev/null
    volumes:
      # Data written to /data inside the container persists in 'app_storage_volume'
      - app_storage_volume:/data

volumes:
  app_storage_volume:
